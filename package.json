{
  "name": "someones-pc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @spc/web dev",
    "build": "pnpm --filter @spc/web build",
    "start": "pnpm --filter @spc/web start",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "pnpm --filter @spc/web test:e2e",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "eslint . && prettier --check .",
    "fix": "eslint . --fix && prettier --write .",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "ingest": "pnpm --filter @spc/dex ingest",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:push": "pnpm --filter @spc/web db:push",
    "db:seed": "pnpm --filter @spc/web db:seed",
    "setup": "pnpm install && pnpm db:up && pnpm db:push && pnpm db:seed"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/node": "^22.19.0",
    "eslint": "^9.39.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.0",
    "vitest": "^3.2.4"
  }
}
